allprojects {
  tasks.matching { it.name == "test" || it instanceof Test }.configureEach {
    // Force test tasks to run (injected by Spock Test Runner extension)
    outputs.upToDateWhen { false }
  }
}
